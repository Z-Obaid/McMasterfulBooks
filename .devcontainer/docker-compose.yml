version: "4.0"

services:
  dev:
    image: mcr.microsoft.com/devcontainers/typescript-node:1-20-bookworm
    user: root
    privileged: true
    volumes:
      - ../..:/workspaces:cached
    command: sleep infinity